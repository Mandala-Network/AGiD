/**
 * Service Module
 *
 * Previously contained the unified AGIdentityService factory.
 * Components are now assembled directly in start.ts / AGIdentityGateway.
 */

// No exports — retained as empty barrel to avoid breaking directory structure
